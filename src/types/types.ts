export type userInfo = {
    id:string,
    usernames:string,
    password:string
};

export type userObject = {
    _id:string,
    username:string,
    email:string,
    isVerified:boolean,
    isAdmin:boolean
}